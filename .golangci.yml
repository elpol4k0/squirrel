run:
  go: "1.24"
  timeout: 5m

linters:
  enable-all: false
  disable-all: true
  enable:
    - errcheck
    - gosimple
    - govet
    - ineffassign
    - staticcheck
    - unused

issues:
  exclude-rules:
    - path: "_test\\.go"
      linters:
        - errcheck
        - staticcheck
